{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "./mvnw clean package -DskipTests -q -B"
  },
  "deploy": {
    "startCommand": "java -Xmx400m -Xms200m -Dserver.port=$PORT -Dspring.profiles.active=railway -jar target/*.jar",
    "healthcheckPath": "/",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
